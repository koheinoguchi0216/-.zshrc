# ~.zshrc設定

## oh my zsh インストール

'$ curl -L https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh | sh'

## 現在のログインシェルを確認

'echo $SHELL'

## 設定ファイルの作成

vi ~/.zshrc

## 設定ファイルの反映

source ~/.zshrc
